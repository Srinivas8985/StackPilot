global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'stackpilot_backend'
    metrics_path: '/api/metrics'
    # Use host.docker.internal to reach the Node.js backend running natively on the host (port 5000)
    static_configs:
      - targets: ['host.docker.internal:5000']
